# Baseline-Model-HW
Mini NLP project comparing classical text classification baselines (Count, TF-IDF, NB, Logistic Regression, LinearSVC) on a 20-topic dataset. Includes model evaluation using accuracy and macro F1, deployment recommendation, and reproducible Python workflow.
